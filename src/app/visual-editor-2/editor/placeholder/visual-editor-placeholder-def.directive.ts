import { Directive } from '@angular/core';

/** Directive to  define a placeholder for the visual editor. */
@Directive({
  selector: '[aorVisualEditorPlaceholderDef]',
})
export class VisualEditorPlaceholderDefDirective {}
